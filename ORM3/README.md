# OR-Mapper

This OR Mapper (ORM) works with PostgreSQL databases.
Use "DB_Create_Script.sql" to create the database.
Set "OR_Mapper.App" as startup project.
Adjust the Db.Schema and Db.Connection variables in Program.cs (line 12 & 13) and run the program.

For further details, see XML comments in source code.